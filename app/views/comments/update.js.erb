if ($('.alert').length) {
	$('.alert').remove();
}

$('#edit-comment-<%=@comment.id%>').replaceWith("<%= escape_javascript render(:partial => '/comments/comments', :locals => { :comment => @comment }) %>");

<% if !@cancel %>
	var html = "<div class='comment-update-alert alert alert-success'> Comment was updated!</div>";
	$(html).insertBefore('#comment-<%=@comment.id%>');
<% end %>
MathJax.Hub.Queue(["Typeset",MathJax.Hub]);

